package config

const (
	ServiceName = "fireglass-blackbox-tester"

	// Admin
	HealthCheckEndpoint = "/health"
	InfoEndpoint        = "/info"
	ShutdownEndpoint    = "/shutdown"
	MetricsEndpoint     = "/metrics"
)
